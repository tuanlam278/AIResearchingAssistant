# URL của FastAPI backend
VITE_API_URL=http://localhost:8000

# Upload limit shown by the frontend (must match backend MAX_UPLOAD_MB)
VITE_MAX_UPLOAD_MB=50

# Google Identity Services OAuth client ID
VITE_GOOGLE_CLIENT_ID=
